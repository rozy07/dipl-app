%h1 Listing theses

%table.table.table-stripped
  %tr
    %th Name
    %th Faculty
    %th Language
    %th Defended
    %th
    %th
    %th

  - @theses.each do |thesis|
    %tr
      %td= thesis.name
      %td= thesis.faculty.name
      %td= thesis.language
      %td= thesis.defended
      %td= link_to 'Show', thesis, class:'btn btn-success'
      %td= link_to 'Edit', edit_thesis_path(thesis), class:'btn btn-success'
      %td= link_to 'Destroy', thesis, :method => :delete, :data => { :confirm => 'Are you sure?' }, class:'btn btn-success'

%br
= link_to 'New Thesis', new_thesis_path, class:'btn btn-primary'
